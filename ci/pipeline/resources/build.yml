resources:
  - name: build-linux-amd64
    type: s3
    source:
      endpoint:              (( grab meta.aws.endpoint || ~ ))
      skip_ssl_verification: (( grab meta.aws.insecure || false ))
      access_key_id:         (( grab meta.aws.access_key ))
      secret_access_key:     (( grab meta.aws.secret_key ))
      region_name:           (( grab meta.aws.region_name ))
      bucket:                (( grab meta.aws.bucket ))
      regexp:                (( concat meta.name "/build/" meta.name "-linux-amd64-(.*)\.tar\.gz" ))

  - name: build-linux-arm64
    type: s3
    source:
      endpoint:              (( grab meta.aws.endpoint || ~ ))
      skip_ssl_verification: (( grab meta.aws.insecure || false ))
      access_key_id:         (( grab meta.aws.access_key ))
      secret_access_key:     (( grab meta.aws.secret_key ))
      region_name:           (( grab meta.aws.region_name ))
      bucket:                (( grab meta.aws.bucket ))
      regexp:                (( concat meta.name "/build/" meta.name "-linux-arm64-(.*)\.tar\.gz" ))

  - name: build-darwin-amd64
    type: s3
    source:
      endpoint:              (( grab meta.aws.endpoint || ~ ))
      skip_ssl_verification: (( grab meta.aws.insecure || false ))
      access_key_id:         (( grab meta.aws.access_key ))
      secret_access_key:     (( grab meta.aws.secret_key ))
      region_name:           (( grab meta.aws.region_name ))
      bucket:                (( grab meta.aws.bucket ))
      regexp:                (( concat meta.name "/build/" meta.name "-darwin-amd64-(.*)\.tar\.gz" ))

  - name: build-darwin-arm64
    type: s3
    source:
      endpoint:              (( grab meta.aws.endpoint || ~ ))
      skip_ssl_verification: (( grab meta.aws.insecure || false ))
      access_key_id:         (( grab meta.aws.access_key ))
      secret_access_key:     (( grab meta.aws.secret_key ))
      region_name:           (( grab meta.aws.region_name ))
      bucket:                (( grab meta.aws.bucket ))
      regexp:                (( concat meta.name "/build/" meta.name "-darwin-arm64-(.*)\.tar\.gz" ))

  - name: build-workdir
    type: s3
    source:
      endpoint:              (( grab meta.aws.endpoint || ~ ))
      skip_ssl_verification: (( grab meta.aws.insecure || false ))
      access_key_id:         (( grab meta.aws.access_key ))
      secret_access_key:     (( grab meta.aws.secret_key ))
      region_name:           (( grab meta.aws.region_name ))
      bucket:                (( grab meta.aws.bucket ))
      regexp:                (( concat meta.name "/workdir/" meta.name "-workdir-(.*)\.tar\.gz" ))
